import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#585dcc", // Cambia este color por tu color primario personalizado
    },
    secondary: {
      main: "#888888", // Cambia este color por tu color secundario personalizado
    },
    error: {
      main: "#bb444e", // Cambia este color por tu color de error personalizado
    },
    success: {
      main: "#478d29", // Cambia este color por tu color de éxito personalizado
    },
    warning: {
      main: "#f0a500", // Cambia este color por tu color de advertencia personalizado
    },
  },
});
